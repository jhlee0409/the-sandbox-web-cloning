import { style } from "@vanilla-extract/css";

export const flexBox = style({
  display: "flex",
});

export const flexColumBox = style({
  display: "flex",
  flexDirection: "column",
});

export const AlignCenter = style({
  alignItems: "center",
});

export const JustifyCenter = style({
  justifyContent: "center",
});
